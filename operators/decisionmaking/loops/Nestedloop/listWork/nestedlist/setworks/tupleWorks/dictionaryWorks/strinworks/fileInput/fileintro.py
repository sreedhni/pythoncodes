#   modes

# read(r)  >> to read file content
# write(w)  >> to add  acontent already ulla content pokum
# append(a) >> to add a content without removing the old content\

#  we want to call open fnction 

# open(path,mode)

# it used to  write read append a txt file into a python file